import styles from "@/styles/sections.module.css";

export default function About() {
  return (
    <section id="about" className={styles.section}>
      <h2>About</h2>
      <p>
        I build intelligent systems at the intersection of AI, infrastructure,
        and product design. Cold logic. Clean execution.
      </p>
    </section>
  );
}